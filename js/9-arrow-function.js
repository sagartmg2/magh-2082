/* 
        function double(number) {
            console.log(`2 X ${number} = ${2 * number}`);
        }

        double(20)
        double(29) 
*/

// arrow function

const double = (number) => {
  console.log(`2 X ${number} = ${2 * number}`);
};

double(20);
double(29);
